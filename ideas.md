# ایده‌ها و پیشنهادات برای افزونه اسپات پلیر

---

## امنیت

1. ~~**رفع XSS در order-box.php**~~
   > ✅ **پیاده‌سازی شد — نسخه 20.9**

2. ~~**حذف `verify => false` در API**~~
   > ✅ **پیاده‌سازی شد — نسخه 20.9**

3. ~~**نانس برای ذخیره سفارش**~~
   > ✅ **پیاده‌سازی شد — نسخه 21.0**

4. ~~**جلوگیری از Path Traversal در CSV**~~
   > ✅ **پیاده‌سازی شد — نسخه 21.0**

5. **محدودسازی نرخ درخواست API**: هیچ rate limiting روی endpoint های `/spotx` و `/spdeb` وجود نداره.
   > **تأثیر بر سرعت:** تأثیر منفی جزئی روی پلیر وب. endpoint `/spotx` هر بار که پلیر وب نیاز به تجدید cookie داره صدا زده می‌شه. اگه rate limiting با transient پیاده بشه، یه DB read کوچک به هر درخواست اضافه می‌شه که می‌تونه ۵–۱۰ms تأخیر ایجاد کنه. برای فرانت‌اند عادی و پنل ادمین بی‌تأثیره.

---

## فیچرهای جدید

### مدیریت لایسنس

6. **لغو خودکار لایسنس پس از استرداد**: وقتی وضعیت سفارش به `refunded` تغییر می‌کنه، لایسنس رو به‌صورت خودکار غیرفعال کن (صفر کردن device ها).
   > **تأثیر بر سرعت:** بدون تأثیر روی کاربران عادی یا فرانت‌اند. فقط لحظه‌ای که ادمین سفارش رو مسترد می‌کنه یه API call اضافه می‌شه که چند صدم ثانیه طول می‌کشه. برای checkout و صفحات عمومی سایت کاملاً بی‌تأثیره.

7. **تمدید لایسنس**: دکمه‌ای در صفحه سفارش ادمین که تاریخ انقضای لایسنس رو از طریق API تمدید کنه.
   > **تأثیر بر سرعت:** خنثی در همه حالت‌ها. یه API call on-demand هنگام کلیک دکمه در ادمین. کاربران فرانت‌اند اصلاً درگیر نمی‌شند.

8. **نمایش تعداد دستگاه‌های فعال**: در باکس سفارش ادمین تعداد دستگاه‌هایی که لایسنس را فعال کرده‌اند نشان بده.
   > **تأثیر بر سرعت:** تأثیر منفی قابل‌توجه روی صفحه ویرایش سفارش در ادمین (اگه بدون caching پیاده بشه). برای هر بار باز شدن صفحه سفارش، یه API call به اسپات پلیر زده می‌شه که ۳۰۰–۸۰۰ms تأخیر ایجاد می‌کنه. راه‌حل: باید با AJAX lazy load یا transient با TTL کوتاه (~۵ دقیقه) پیاده بشه. برای فرانت‌اند و کاربران عادی بی‌تأثیره.

9. **Webhook از اسپات پلیر**: یه endpoint در وردپرس بساز که رویدادهای اسپات پلیر (مثل فعال‌سازی دستگاه) رو دریافت و در متادیتای سفارش ذخیره کنه.
   > **تأثیر بر سرعت:** خنثی برای کاربران سایت. فقط وقتی اسپات پلیر webhook می‌فرسته یه WordPress bootstrap اجرا می‌شه که روی سرور باره، نه روی تجربه کاربر. یه DB write ساده هم انجام می‌شه.

### تجربه کاربر

10. **ارسال کلید لایسنس در ایمیل**: پس از ساخت لایسنس، کلید رو در ایمیل تأییدیه سفارش ووکامرس یا یه ایمیل جداگانه بفرست.
    > **تأثیر بر سرعت:** اگه با WP Cron یا در پس‌زمینه انجام بشه: خنثی. اگه sync باشه: می‌تونه چند صدم ثانیه به زمان پردازش سفارش اضافه کنه، ولی کاربر این رو احساس نمی‌کنه چون صفحه تأیید سفارش قبلاً لود شده. برای فرانت‌اند عادی بی‌تأثیره.

11. **نمایش تاریخ انقضا به کاربر**: اگه API تاریخ انقضا برمی‌گردونه، در صفحه دوره‌های کاربر نمایش بده.
    > **تأثیر بر سرعت:** کاملاً بستگی به پیاده‌سازی داره. اگه تاریخ انقضا را در متادیتا ذخیره کنیم (هنگام ساخت لایسنس): خنثی، فقط یه DB read ساده‌ست. اگه برای نمایش API call بزنیم: تأثیر منفی جدی روی صفحه دوره‌های کاربر برای همه کاربران (۳۰۰–۸۰۰ms به ازای هر لایسنس). پیاده‌سازی صحیح: ذخیره local.

12. **غیرفعال‌سازی دستگاه توسط کاربر**: اگه API امکانش رو داشته باشه، کاربر بتونه از حساب کاربری‌اش یه دستگاه رو غیرفعال کنه.
    > **تأثیر بر سرعت:** خنثی روی صفحات عادی. فقط لحظه کلیک کاربر یه API call اجرا می‌شه. هیچ باری روی فرانت‌اند منفعلانه نداره.

13. ~~**صفحه‌بندی در شورت‌کد**~~
    > ✅ **پیاده‌سازی شد — نسخه 20.7**

### عملیات دسته‌ای

14. ~~**پردازش ناهمزمان bulk**~~
    > ✅ **پیاده‌سازی شد — نسخه 20.8**

15. ~~**نوار پیشرفت AJAX**~~
    > ✅ **پیاده‌سازی شد — نسخه 21.3** (polling زنده وضعیت صف هر ۵ ثانیه، بدون reload)

16. **خروجی CSV لایسنس‌ها**: دکمه‌ای که لایسنس‌های یه محصول یا بازه زمانی رو با شناسه لایسنس، نام کاربر و شماره سفارش به CSV خروجی بده.
    > **تأثیر بر سرعت:** تأثیر منفی موقتی در ادمین هنگام export. بسته به تعداد سفارشات، می‌تونه یه کوئری سنگین ۲–۱۰ ثانیه‌ای روی DB باشه. باید با streaming یا pagination پیاده بشه. برای فرانت‌اند و کاربران عادی بی‌تأثیره.

17. **فعال‌سازی دسته‌ای لایسنس‌ها**: برعکس غیرفعال‌سازی فعلی، امکان فعال‌سازی مجدد از CSV.
    > **تأثیر بر سرعت:** مشابه عملیات bulk موجود. تأثیر منفی موقتی فقط هنگام اجرا در ادمین. توصیه می‌شه با ایده ۱۴ (ناهمزمان) ترکیب بشه. کاربران فرانت‌اند اصلاً تأثیر نمی‌بینند.

18. **ساخت لایسنس مستقیم (بدون سفارش ووکامرس)**: فرم جداگانه‌ای برای ساخت لایسنس با شناسه دوره و مشخصات دستی، بدون اینکه نیاز به سفارش ووکامرس باشه.
    > **تأثیر بر سرعت:** خنثی کامل. فقط یه فرم ادمین با یه API call on-demand. برای فرانت‌اند و بقیه صفحات بی‌تأثیره.

### داشبورد و گزارش

19. ~~**ویجت داشبورد**~~
    > ✅ **پیاده‌سازی شد — نسخه 21.9** (با transient 15 دقیقه‌ای)

20. ~~**ستون لایسنس در لیست سفارشات**~~
    > ✅ **پیاده‌سازی شد — نسخه 21.2** (دکمه کپی لایسنس به جای نمایش متن کامل)

21. **فیلتر سفارشات بر اساس وضعیت لایسنس**: در صفحه سفارشات، بتونی سفارشاتی که هنوز لایسنس ندارند رو فیلتر کنی.
    > **تأثیر بر سرعت:** تأثیر منفی جزئی فقط هنگام استفاده از فیلتر. یه meta query پیچیده‌تر از حالت عادی اجرا می‌شه. اگه فیلتر استفاده نشه هیچ overhead ای نیست. می‌تونه با اضافه کردن یه DB index روی `_spotplayer_data` بهینه بشه. فرانت‌اند بدون تأثیر.

22. **لاگ رویدادهای لایسنس**: یه جدول ساده در ادمین که log ساخت/خطاهای لایسنس رو با تاریخ و شناسه سفارش نگه داره.
    > **تأثیر بر سرعت:** تأثیر منفی بسیار جزئی هنگام ساخت/خطای لایسنس (یه INSERT). صفحه مشاهده لاگ در ادمین یه SELECT ساده‌ست. برای فرانت‌اند و کاربران عادی کاملاً بی‌تأثیره. با گذشت زمان اگه لاگ‌ها تمیز نشند جدول بزرگ می‌شه، پس نیاز به retention policy داره.

### یکپارچه‌سازی

23. **WooCommerce Subscriptions**: وقتی اشتراک تجدید می‌شه، لایسنس رو تمدید کن؛ وقتی لغو می‌شه، غیرفعالش کن.
    > **تأثیر بر سرعت:** بدون تأثیر روی کاربران. هر دو عملیات (تمدید و لغو) توسط WP Cron در پس‌زمینه اجرا می‌شند. یه API call اضافه در جریان پردازش renewal. فرانت‌اند اصلاً درگیر نمی‌شه.

24. **WooCommerce Memberships**: دسترسی به لایسنس رو به عضویت گره بزن.
    > **تأثیر بر سرعت:** تأثیر منفی جزئی روی فرانت‌اند. هر بار که کاربر صفحه دوره‌ها رو باز می‌کنه یه چک عضویت اضافه می‌شه. اگه Memberships API کند باشه این تأثیر محسوس‌تره. می‌تونه ۲۰–۱۰۰ms اضافه کنه به صفحه دوره‌های کاربر.

25. **WP-CLI**: کامندهایی مثل `wp spotplayer create-license <order_id>` و `wp spotplayer disable-license <license_id>` برای سناریوهای اتوماسیون.
    > **تأثیر بر سرعت:** صفر. CLI فقط از خط فرمان اجرا می‌شه و هیچ overhead ای روی وب‌سایت نداره. در واقع می‌تونه به عنوان ابزار مدیریت offline بدون بار روی وب سرور استفاده بشه.

26. **REST API**: یه endpoint مثل `GET /wp-json/spotplayer/v1/license/<order_id>` برای ادغام با سیستم‌های خارجی.
    > **تأثیر بر سرعت:** تأثیر منفی بسیار جزئی. ثبت route جدید چند میکروثانیه به bootstrap وردپرس اضافه می‌کنه. هنگام فراخوانی endpoint یه WP bootstrap کامل + DB query اجرا می‌شه. برای فرانت‌اند و صفحات عادی کاملاً بی‌تأثیره.

---

## بهبود و بهینه‌سازی

### کد و معماری

27. **جایگزینی `eval()`**: تابع `spot_woo_license_data_eval` کد PHP رو با `eval()` اجرا می‌کنه. می‌شه از یه closure یا parser محدود استفاده کرد، یا حداقل خروجی رو بیشتر validate کرد.
    > **تأثیر بر سرعت:** تأثیر مثبت جزئی در همه جایی که لایسنس ساخته می‌شه. `eval()` نمی‌تونه توسط PHP OPcache کمپایل و کش بشه، یعنی هر بار مجدداً parse می‌شه. جایگزینی با closure کد رو OPcache-friendly می‌کنه.

28. **کشینگ با Transient**: نتیجه `spot_request_license_get` رو برای مدت کوتاهی (مثلاً ۵ دقیقه) در transient ذخیره کن تا تعداد درخواست‌های API کم بشه.
    > **تأثیر بر سرعت:** تأثیر مثبت قابل‌توجه روی پنل ادمین. هر بار که صفحه سفارش باز می‌شه به جای یه API call خارجی (۳۰۰–۸۰۰ms) از cache می‌خونه (زیر ۵ms).

29. **Retry خودکار**: اگه ساخت لایسنس با خطای شبکه شکست خورد (نه خطای منطقی)، یه بار دیگه با تأخیر کوتاه امتحان کن.
    > **تأثیر بر سرعت:** در حالت عادی (بدون خطا): خنثی. در صورت خطای شبکه: تأخیر ۱–۳ ثانیه به پردازش سفارش اضافه می‌کنه.

30. ~~**Background license creation**~~
    > ✅ **پیاده‌سازی شد — نسخه 20.7**

31. **PHP 8 Typed Properties**: تابع‌ها type hint ندارند؛ اضافه کردن return type و parameter type کیفیت کد رو بالا می‌بره.
    > **تأثیر بر سرعت:** خنثی کامل در runtime.

### تجربه توسعه‌دهنده/ادمین

32. **بررسی اعتبار API key**: در صفحه تنظیمات، یه دکمه «تست اتصال» باشه که بلافاصله وضعیت کلید API رو نشون بده.
    > **تأثیر بر سرعت:** خنثی کامل. یه AJAX call on-demand فقط وقتی ادمین دکمه رو می‌زنه.

33. ~~**نمایش خطاهای API واضح‌تر**~~
    > ✅ **پیاده‌سازی شد — نسخه 21.1**

34. **راهنمای inline برای کد سفارشی**: در textarea کد ساخت لایسنس، یه syntax highlighter ساده (مثل CodeMirror) UX رو خیلی بهتر می‌کنه.
    > **تأثیر بر سرعت:** تأثیر منفی جزئی فقط روی صفحه تنظیمات ادمین. CodeMirror حدود ۳۰–۵۰KB (gzipped) هست که فقط در صفحه تنظیمات لود می‌شه.

35. **اعتبارسنجی شناسه دوره در محصول**: قبل از ذخیره، یه درخواست به API بزن و بررسی کن شناسه وارد شده واقعاً وجود داره.
    > **تأثیر بر سرعت:** تأثیر منفی جزئی هنگام ذخیره محصول در ادمین. یه API call اضافه به اسپات پلیر (۳۰۰–۸۰۰ms) به زمان ذخیره محصول اضافه می‌کنه.

36. **Multisite support**: بررسی و مستندسازی رفتار افزونه در WordPress Multisite.
    > **تأثیر بر سرعت:** خنثی تا تأثیر مثبت جزئی.

---

## جدول اولویت‌بندی

> **راهنمای ستون تأثیر سرعت:**
> - 🟢 مثبت: پیاده‌سازی سرعت رو بهبود می‌ده
> - 🔴 منفی: پیاده‌سازی سرعت رو کاهش می‌ده (باید با دقت پیاده بشه)
> - ⚪ خنثی: تأثیری روی سرعت نداره

| اولویت | # | عنوان | دسته | تأثیر سرعت | کجا تأثیر می‌ذاره | دلیل اولویت |
|:---:|:---:|---|:---:|:---:|---|---|
| ۱ | 6 | لغو خودکار لایسنس پس از استرداد | مدیریت لایسنس | ⚪ خنثی | ادمین - لحظه استرداد | جلوگیری از دسترسی بعد از بازپرداخت |
| ۲ | 28 | کشینگ با Transient | بهینه‌سازی | 🟢 مثبت قابل‌توجه | ادمین - صفحه سفارش | کاهش API call از ۵۰۰ms به زیر ۵ms |
| ۳ | 32 | بررسی اعتبار API key | UX ادمین | ⚪ خنثی | ادمین - on-demand | پرتکرارترین مشکل راه‌اندازی اولیه |
| ۴ | 29 | Retry خودکار | بهینه‌سازی | ⚪ خنثی (حالت عادی) | فرانت‌اند - لحظه خطا | کاهش سفارشات بدون لایسنس ناشی از خطای شبکه |
| ۵ | 27 | جایگزینی `eval()` | معماری | 🟢 مثبت جزئی | همه جا - هر ساخت لایسنس | OPcache-friendly شدن + کاهش ریسک امنیتی |
| ۶ | 22 | لاگ رویدادهای لایسنس | گزارش | ⚪ خنثی | ادمین | قابلیت دیباگ و پیگیری خطاها |
| ۷ | 10 | ارسال کلید لایسنس در ایمیل | تجربه کاربر | ⚪ خنثی (با async) | — | کاربر بدون ورود به سایت به لایسنس دسترسی داره |
| ۸ | 16 | خروجی CSV لایسنس‌ها | عملیات دسته‌ای | 🔴 منفی موقتی | ادمین - هنگام export | نیاز عملی برای گزارش‌گیری |
| ۹ | 21 | فیلتر سفارشات بر اساس وضعیت لایسنس | گزارش | 🔴 منفی جزئی on-demand | ادمین - هنگام فیلتر | شناسایی سفارشات بدون لایسنس |
| ۱۰ | 5 | محدودسازی نرخ درخواست API | امنیت | 🔴 منفی جزئی | فرانت‌اند - پلیر وب | ریسک پایین‌تر از موارد قبلی |
| ۱۱ | 35 | اعتبارسنجی شناسه دوره در محصول | UX ادمین | 🔴 منفی جزئی | ادمین - ذخیره محصول | جلوگیری از ذخیره شناسه اشتباه |
| ۱۲ | 18 | ساخت لایسنس مستقیم بدون سفارش | عملیات دسته‌ای | ⚪ خنثی | ادمین - on-demand | کاربرد برای آموزشگاه‌های آفلاین |
| ۱۳ | 11 | نمایش تاریخ انقضا به کاربر | تجربه کاربر | 🟢/🔴 بستگی به پیاده‌سازی | فرانت‌اند - صفحه دوره‌ها | اگه local ذخیره بشه خنثی، اگه API call بزنه مشکل‌ساز |
| ۱۴ | 7 | تمدید لایسنس | مدیریت لایسنس | ⚪ خنثی | ادمین - on-demand | نیاز به پشتیبانی API |
| ۱۵ | 26 | REST API | یکپارچه‌سازی | ⚪ خنثی | — | برای سایت‌هایی با نیاز به ادغام خارجی |
| ۱۷ | 9 | Webhook از اسپات پلیر | یکپارچه‌سازی | ⚪ خنثی | — | نیاز به پشتیبانی سمت سرور اسپات پلیر |
| ۱۸ | 17 | فعال‌سازی دسته‌ای لایسنس‌ها | عملیات دسته‌ای | 🔴 منفی موقتی | ادمین - هنگام اجرا | کاربرد کمتر از غیرفعال‌سازی |
| ۱۹ | 8 | نمایش تعداد دستگاه‌های فعال | مدیریت لایسنس | 🔴 منفی زیاد (بدون caching) | ادمین - صفحه سفارش | **فقط با AJAX lazy load یا transient قابل قبوله** |
| ۲۰ | 12 | غیرفعال‌سازی دستگاه توسط کاربر | تجربه کاربر | ⚪ خنثی | فرانت‌اند - on-demand | نیاز به API مناسب |
| ۲۱ | 25 | WP-CLI | یکپارچه‌سازی | ⚪ خنثی (CLI فقط) | — | هیچ تأثیری روی وب‌سایت نداره |
| ۲۲ | 34 | Syntax highlighter کد لایسنس | UX ادمین | 🔴 منفی جزئی | ادمین - صفحه تنظیمات | بهبود ظاهری با هزینه کمی |
| ۲۳ | 31 | PHP 8 Typed Properties | معماری | ⚪ خنثی | — | بهبود کیفیت کد بدون تأثیر runtime |
| ۲۴ | 24 | WooCommerce Memberships | یکپارچه‌سازی | 🔴 منفی جزئی | فرانت‌اند - صفحه دوره‌ها | وابسته به داشتن افزونه Memberships |
| ۲۵ | 36 | Multisite support | معماری | ⚪ خنثی | — | کاربرد خاص |
| ۲۶ | 23 | WooCommerce Subscriptions | یکپارچه‌سازی | ⚪ خنثی (background) | — | وابسته به داشتن افزونه Subscriptions |
